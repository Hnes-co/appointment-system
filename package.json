{
  "name": "react-appointment-system",
  "version": "1.0.2",
  "main": "dist/AppointmentSystem.js",
  "author": "Hannes Pohjola",
  "private": false,
  "license": "MIT",
  "repository": "Hnes-co/appointment-system",
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "peerDependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist &&  babel src/ -d dist --copy-files",
    "build:win": "rd /s /q dist && mkdir dist &&  babel src/ -d dist --copy-files"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.19.3",
    "@babel/preset-react": "^7.18.6",
    "babel-core": "^6.26.3"
  },
  "files": [
    "dist"
  ]
}
